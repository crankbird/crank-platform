# 🚢 Crank Platform Port Configuration
# Environment variables for flexible service deployment
# 
# Usage:
#   cp .env.template .env
#   Edit .env with your desired ports
#   docker-compose up

# =============================================================================
# 🏢 PLATFORM CORE SERVICES (8000-8099)
# =============================================================================

# Main Platform Service  
PLATFORM_HTTP_PORT=8000
PLATFORM_HTTPS_PORT=8443

# Gateway Service
GATEWAY_PORT=8080

# =============================================================================  
# 📄 DOCUMENT PROCESSING SERVICES (8100-8199)
# =============================================================================

# CrankDoc Mesh Service
CRANKDOC_MESH_PORT=8100
CRANKDOC_SERVICE_PORT=8101

# =============================================================================
# 📧 EMAIL PROCESSING SERVICES (8200-8399) 
# =============================================================================

# CrankEmail Mesh Service
CRANKEMAIL_MESH_PORT=8200
CRANKEMAIL_SERVICE_PORT=8201

# =============================================================================
# 🖼️  IMAGE PROCESSING SERVICES (8400-8499)
# =============================================================================

# Image Classifier (CPU)
CLASSIFIER_HTTP_PORT=8400
CLASSIFIER_HTTPS_PORT=8401

# Image Classifier (GPU)
GPU_CLASSIFIER_HTTP_PORT=8500
GPU_CLASSIFIER_HTTPS_PORT=8501

# =============================================================================
# 🔍 DIAGNOSTIC SERVICES (8600-8699)
# =============================================================================

# Mesh Diagnostics
DIAGNOSTICS_PORT=8600

# =============================================================================
# 🌊 STREAMING SERVICES (8500-8599) [LEGACY SECTION - GPU moved to 8500s]
# =============================================================================

# Streaming Service (SSE, WebSocket, Real-time)
STREAMING_PORT=8500
STREAMING_HOST=0.0.0.0

# =============================================================================
# 🚀 GPU/AI SERVICES (8600-8699) 
# =============================================================================

# GPU Image Classifier (CUDA acceleration)
GPU_IMAGE_CLASSIFIER_PORT=8600
GPU_IMAGE_CLASSIFIER_HOST=0.0.0.0
GPU_IMAGE_CLASSIFIER_HTTPS_PORT=8601

# =============================================================================
# 🔧 DEVELOPMENT OVERRIDES
# =============================================================================

# Uncomment and modify for development environment
# PLATFORM_PORT=8080
# EMAIL_CLASSIFIER_PORT=8203
# EMAIL_PARSER_PORT=8303

# =============================================================================
# 🏭 PRODUCTION EXAMPLES
# =============================================================================

# Production with load balancer (all services behind reverse proxy)
# PLATFORM_PORT=8000
# DOC_CONVERTER_PORT=8100
# EMAIL_CLASSIFIER_PORT=8200
# EMAIL_PARSER_PORT=8300
# IMAGE_CLASSIFIER_PORT=8400
# STREAMING_PORT=8500

# Production with separate hosts (different IP addresses)
# All services can use same port if on different hosts
# PLATFORM_PORT=8000
# DOC_CONVERTER_PORT=8000  # Different host
# EMAIL_CLASSIFIER_PORT=8000  # Different host
# EMAIL_PARSER_PORT=8000  # Different host

# =============================================================================
# ☁️ KUBERNETES DEPLOYMENT
# =============================================================================

# For Kubernetes, use service discovery instead of ports
# These values become container ports, not exposed ports
# The load balancer handles external access

# =============================================================================  
# 🐳 DOCKER SWARM DEPLOYMENT
# =============================================================================

# For Docker Swarm, use overlay networks
# Services communicate by name, not by port
# External access through ingress routing

# =============================================================================
# 📊 PORT ALLOCATION SUMMARY  
# =============================================================================

# Range          | Service Type        | Default Ports
# ---------------|--------------------|--------------
# 8000-8099      | Platform Core      | 8000, 8443
# 8100-8199      | Document Services  | 8100-8101  
# 8200-8299      | Email Classifier   | 8200-8201
# 8300-8399      | Email Parser       | 8300-8301
# 8400-8499      | Image Services     | 8400-8401
# 8500-8599      | Streaming Services | 8500
# 8600-8699      | GPU/AI Services    | 8600-8601
# 8700-8799      | Future Expansion   | TBD
# 8800-8899      | Custom Workers     | TBD
# 8900-8999      | Development/Test   | TBD

# =============================================================================
# 🎯 QUICK START EXAMPLES
# =============================================================================

# All defaults (recommended for first-time setup):
# Leave all variables commented out - defaults will be used

# Development on busy system (avoid common ports):  
# EMAIL_CLASSIFIER_PORT=8287
# EMAIL_PARSER_PORT=8387
# STREAMING_PORT=8587

# Multiple instances on same host:
# Instance 1: Use defaults (8000, 8100, 8200, etc.)
# Instance 2: Add 10 (8010, 8110, 8210, etc.)
# Instance 3: Add 20 (8020, 8120, 8220, etc.)